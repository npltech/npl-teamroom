# Supabase → Project Settings → Database → Connection string → "URI"
# Use the DIRECT connection (port 5432), not the pooler (port 6543) —
# drizzle-kit needs a direct connection to run migrations.
# Replace [YOUR-PASSWORD] with your actual database password.

DATABASE_URL=postgresql://postgres.umatdsjexnbcyetghtki:HrPortal2026X9@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres
